name: Deploy task-app
on:
  push:
    branches: ["master"]


jobs:
  build-deploy:
    #runner works on ubuntu
    runs-on: ubuntu-latest

    #check code from errors
    steps:
      - name: Checkout code
        uses: actions/checkout@v3


      #install java
      - name: Set up JDK21
        uses: actions/setup-java@v3
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven

      #build without tests
      - name: Build with maven
        run: mvn clean package -DskipTests

      - name: Login to docker hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build & push image
        run: |
          docker build -t ${{ secrets.DOCKERHUB_USERNAME }}/task-manager:latest .
          docker push ${{ secrets.DOCKERHUB_USERNAME }}/task-manager:latest

      # I will let this part till server works
#      - name: deploy to k8s using ssh with helm
#        uses: appleboy/ssh-action@master
#        with:
#          host: ${{ secrets.EC2_HOST }}
#          username: ${{ secrets.EC2_USER }}
#          key: ${{ secrets.EC2_SSH_KEY }}
#          script: |
#
#            cd /home/mo3bkreem/TaskApp
#
#            git pull origin master
#
#            kubectl create namespace prod \
#              --dry-run=client -o yaml | kubectl apply -f .
#
#            kubectl create secret generic task-secret \
#              --from-literal=POSTGRES_PASSWORD="${{ secrets.POSTGRES_PASSWORD }}" \
#              -n prod \
#              --dry-run=client -o yaml | kubectl apply -f -
#
#
#            helm upgrade --install task-app . \
#            -n prod \
#            --create-namespace
#

