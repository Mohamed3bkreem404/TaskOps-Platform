apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: pod-reader-binding

subjects:
  - kind: ServiceAccount
    name: task-app-sa
    namespace: prod

roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: pod-reader